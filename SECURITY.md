# Security Policy

EvaCode 可以读取和写入文件、执行命令、连接 MCP Server，并启动其他 Agent。请把它当作
具有本地开发者权限的程序，而不是普通聊天网页。

## 安全部署建议

- 使用普通用户运行，不要以 root 身份运行。
- 默认使用 `permission_mode: default`。
- 把工作目录限制在可恢复、已备份的项目范围内。
- 使用环境变量保存 API Key，不要提交本地配置、日志、会话或记忆。
- 审查第三方 MCP Server、Skill、Hook 和 Agent 定义后再启用。
- Remote UI 当前没有内置身份认证。不要把 18888 端口直接暴露到公网；优先使用防火墙、
  SSH 隧道或带认证的反向代理。
- 对重要环境使用容器、虚拟机或 OS 沙箱提供额外隔离。

## 支持范围

这是学习和实验项目，不承诺生产级安全支持。公开部署或处理敏感代码前，应自行进行威胁
建模、最小权限配置和安全测试。

## 报告漏洞

请不要在公开 Issue 中粘贴 API Key、内部地址、私有源码或可直接利用的敏感细节。
如仓库启用了 GitHub Private Vulnerability Reporting，请通过仓库的 Security 页面提交
私密报告；否则先创建不包含利用细节的安全 Issue，请维护者提供私密沟通渠道。

报告中建议包含：

- 受影响版本或提交。
- 复现所需的最小配置。
- 预期行为和实际行为。
- 可能的影响范围。
- 已进行的缓解措施。
